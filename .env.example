# Backend base URL — used by the better-auth browser client.
# In development this defaults to http://localhost:3000 (Vite proxy handles /api/* app calls).
# Set this in production to your Render backend URL.
VITE_API_URL=http://localhost:3000
